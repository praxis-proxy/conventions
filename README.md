# Praxis Conventions

This is a template repository that contains development conventions, rules,
tooling, and other groundwork to scaffold a new project.

The `conventions-probe` crate is a minimal workspace member that keeps every
quality gate (build, lint, doc, test, coverage, audit) verifiable against real
code. Replace it with real crates when scaffolding a project.
